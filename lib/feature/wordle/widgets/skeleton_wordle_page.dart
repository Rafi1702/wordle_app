import 'package:flutter/material.dart';

class WordlePageSkeleton extends StatelessWidget {
  const WordlePageSkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
